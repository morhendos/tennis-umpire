diff --git a/node_modules/react-native-flic2/android/build.gradle b/node_modules/react-native-flic2/android/build.gradle
index e475d29..43543c0 100644
--- a/node_modules/react-native-flic2/android/build.gradle
+++ b/node_modules/react-native-flic2/android/build.gradle
@@ -10,10 +10,10 @@
 //   original location:
 //   - https://github.com/facebook/react-native/blob/0.58-stable/local-cli/templates/HelloWorld/android/app/build.gradle
 
-def DEFAULT_COMPILE_SDK_VERSION = 28
-def DEFAULT_BUILD_TOOLS_VERSION = '28.0.3'
+def DEFAULT_COMPILE_SDK_VERSION = 34
+def DEFAULT_BUILD_TOOLS_VERSION = '34.0.0'
 def DEFAULT_MIN_SDK_VERSION = 23
-def DEFAULT_TARGET_SDK_VERSION = 28
+def DEFAULT_TARGET_SDK_VERSION = 34
 
 def safeExtGet(prop, fallback) {
     rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
@@ -35,9 +35,9 @@ buildscript {
 }
 
 apply plugin: 'com.android.library'
-apply plugin: 'maven-publish'
 
 android {
+    namespace "nl.xguard.flic2"
     compileSdkVersion safeExtGet('compileSdkVersion', DEFAULT_COMPILE_SDK_VERSION)
     buildToolsVersion safeExtGet('buildToolsVersion', DEFAULT_BUILD_TOOLS_VERSION)
     defaultConfig {
@@ -54,96 +54,17 @@ android {
 repositories {
     // ref: https://www.baeldung.com/maven-local-repository
     mavenLocal()
-    maven {
-        // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
-        url "$rootDir/../node_modules/react-native/android"
-    }
-    maven {
-        // Android JSC is installed from npm
-        url "$rootDir/../node_modules/jsc-android/dist"
-    }
     maven { url 'https://jitpack.io' }
     google()
 }
 
 dependencies {
     //noinspection GradleDynamicVersion
-    implementation 'com.facebook.react:react-native:+'  // From node_modules
+    implementation 'com.facebook.react:react-android:+'  // From node_modules
     implementation 'com.github.50ButtonsEach:flic2lib-android:1.3.1'
 
     implementation 'io.reactivex.rxjava2:rxandroid:2.1.1'
     implementation 'io.reactivex.rxjava2:rxjava:2.2.13'
 }
 
-def configureReactNativePom(def pom) {
-    def packageJson = new groovy.json.JsonSlurper().parseText(file('../package.json').text)
-
-    pom.project {
-        name packageJson.title
-        artifactId packageJson.name
-        version = packageJson.version
-        group = "nl.xguard.flic2"
-        description packageJson.description
-        url packageJson.repository.baseUrl
-
-        licenses {
-            license {
-                name packageJson.license
-                url packageJson.repository.baseUrl + '/blob/master/' + packageJson.licenseFilename
-                distribution 'repo'
-            }
-        }
-
-        developers {
-            developer {
-                id packageJson.author
-                name packageJson.author
-            }
-        }
-    }
-}
-
-afterEvaluate { project ->
-    // some Gradle build hooks ref:
-    // https://www.oreilly.com/library/view/gradle-beyond-the/9781449373801/ch03.html
-    task androidJavadoc(type: Javadoc) {
-        source = android.sourceSets.main.java.srcDirs
-        classpath += files(android.bootClasspath)
-        project.getConfigurations().getByName('implementation').setCanBeResolved(true)
-        classpath += files(project.getConfigurations().getByName('implementation').asList())
-        include '**/*.java'
-    }
-
-    task androidJavadocJar(type: Jar, dependsOn: androidJavadoc) {
-        classifier = 'javadoc'
-        from androidJavadoc.destinationDir
-    }
-
-    task androidSourcesJar(type: Jar) {
-        classifier = 'sources'
-        from android.sourceSets.main.java.srcDirs
-        include '**/*.java'
-    }
-
-    android.libraryVariants.all { variant ->
-        def name = variant.name.capitalize()
-        def javaCompileTask = variant.javaCompileProvider.get()
-
-        task "jar${name}"(type: Jar, dependsOn: javaCompileTask) {
-            from javaCompileTask.destinationDir
-        }
-    }
-
-    artifacts {
-        archives androidSourcesJar
-        archives androidJavadocJar
-    }
 
-   publishing {
-        repositories {
-            maven {
-                url = uri("${rootProject.projectDir}/maven-repo")
-            }
-        }
-   }
-}
diff --git a/node_modules/react-native-flic2/android/src/main/java/nl/xguard/flic2/Flic2.java b/node_modules/react-native-flic2/android/src/main/java/nl/xguard/flic2/Flic2.java
index bb39e09..de55325 100644
--- a/node_modules/react-native-flic2/android/src/main/java/nl/xguard/flic2/Flic2.java
+++ b/node_modules/react-native-flic2/android/src/main/java/nl/xguard/flic2/Flic2.java
@@ -163,8 +163,14 @@ public class Flic2 extends ReactContextBaseJavaModule implements LifecycleEventL
 
         int permissionCheck = ContextCompat.checkSelfPermission(getReactApplicationContext(), Manifest.permission.ACCESS_FINE_LOCATION);
         if (permissionCheck != PackageManager.PERMISSION_GRANTED) {
-            ReactEvent.getInstance().sendScanStatusMessage("noPermission");
-            return;
+            // Fallback: check for coarse location + BLUETOOTH_SCAN (Android 12+)
+            int coarseCheck = ContextCompat.checkSelfPermission(getReactApplicationContext(), Manifest.permission.ACCESS_COARSE_LOCATION);
+            int btScanCheck = ContextCompat.checkSelfPermission(getReactApplicationContext(), "android.permission.BLUETOOTH_SCAN");
+            Log.d(TAG, "Fine location: " + permissionCheck + ", Coarse: " + coarseCheck + ", BT_SCAN: " + btScanCheck);
+            if (coarseCheck != PackageManager.PERMISSION_GRANTED || btScanCheck != PackageManager.PERMISSION_GRANTED) {
+                ReactEvent.getInstance().sendScanStatusMessage("noPermission");
+                return;
+            }
         }
 
         mReactFlic2Manager.startScan();
diff --git a/node_modules/react-native-flic2/index.js b/node_modules/react-native-flic2/index.js
index 0aab807..def1f0c 100644
--- a/node_modules/react-native-flic2/index.js
+++ b/node_modules/react-native-flic2/index.js
@@ -237,8 +237,8 @@ class Flic2 extends EventEmitter {
    */
   startScan() {
 
-    // listen for scanResult
-    this.nativeEvents.addListener('scanResult', this.onScanResultFunction);
+    // listen for scanResult (store subscription for proper cleanup)
+    this._scanSubscription = this.nativeEvents.addListener('scanResult', this.onScanResultFunction);
 
     // start the scan
     Flic2Module.startScan();
@@ -256,10 +256,13 @@ class Flic2 extends EventEmitter {
    */
   stopScan() {
 
-    // listen for scanResult
-    this.nativeEvents.removeListener('scanResult', this.onScanResultFunction);
+    // remove scanResult listener via subscription
+    if (this._scanSubscription) {
+      this._scanSubscription.remove();
+      this._scanSubscription = null;
+    }
 
-    // start the scan
+    // stop the scan
     Flic2Module.stopScan();
 
     // done
@@ -278,8 +281,11 @@ class Flic2 extends EventEmitter {
     // check if error
     let ButtonObject;
     if (event === 'completion') {
-      // remove listener
-      this.nativeEvents.removeListener('scanResult', this.onScanResultFunction);
+      // remove listener via subscription
+      if (this._scanSubscription) {
+        this._scanSubscription.remove();
+        this._scanSubscription = null;
+      }
 
       if (!!error === false) {
 
